alias la 'ls -a' # Shorthand to get all directories/files
alias lla 'ls -la' # Shorthand to get all directories/files in list format
alias wthr 'curl wttr.in' # Get the weather from wttr.in
alias focus 'timr -m pomodoro -w 45:00 -p 10:00:00 -n on --blink on'
